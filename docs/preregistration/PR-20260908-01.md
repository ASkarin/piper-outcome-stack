# PiPER 实验预注册 PR-20260908-01

- 状态：仿真与 sim2real 核心工作线修订；规划已批准，代码实现待用户另行批准
- 登记日期：2026-09-08
- amends：`PR-20260906-01`；原文完整保留于 [PR-20260906-01.md](PR-20260906-01.md)，作为历史协议而非第二个活动协议
- 修订原因：用户明确仿真与 sim2real 和 ACT 同样重要，并批准同步项目规划；本轮排除代码实现
- 已查看数据：本地实验 registry 仍只有表头；此前已查看基础关节/夹爪联调报告和控制机配置，未有正式 ACT/SmolVLA/sim2real 成功率作为本次决策依据。本轮不运行实验或重新核验远端工件。
- 时间线影响：截止 2027-01-14、任务 A/B 与 10-04 任务 C gate 保留；减少额外消融和 SmolVLA 扩展，为仿真线调配现有容量
- 适用项目：标准 PiPER + 实机 ACT/必要 SmolVLA 对照 + MuJoCo/sim2real + 动作结果预测或失败检测
- 冻结范围：原有核心任务语义、七维策略接口、统计与最终测试禁调项保留；新增 sim2real 研究问题、三组对照及跨域来源/防泄漏规则。数值预算与模型参数须实验前冻结
- 后续补齐：尚未冻结的实机安全/场景字段，以及仿真版本、模型/控制/相机参数、随机化、数据来源、训练和实机回合预算；已有实测身份按记录沿用，不重新猜测。

本预注册是当前唯一活动版本，完整承接上一版本的研究问题、指标、划分和停止规则；不得通过修改本文件覆盖已经运行的正式实验。合法修订使用新 ID，注明 `amends`、原因、已查看数据和时间线影响。

## 1. 研究问题

1. **ACT 实机基线**：在任务 A 上，固定数据版本、观测、动作表示和评测预算后，ACT 能否形成跨 seed 可重复的实机成功率基线？
2. **SmolVLA 适用边界**：在与 ACT 相同的数据、相机、状态、划分和预算下，SmolVLA 是否改善任务 B 的语言改写或 OOD 泛化，同时保持可接受的精细控制和延迟？
3. **动作结果模型**：给定当前观测、机器人状态、任务指令和候选动作块，结果模型能否提前识别有害失败，并在闭环介入时降低有害失败而不过度终止本可成功回合？

4. **sim2real 与真实数据效率**：任务 A 中，在相同实机评测条件与明确真实数据预算下，仿真训练能否直接迁移？仿真预训练加真实微调相对于相同真实数据量的 ACT，是否改善成功率或降低所需真实示范量？

这些是假设而非预设正向结论。SmolVLA、sim2real 或结果模型无稳定收益时保留完整负结果；失败但证据完整可以闭合实验，不能宣称已证明有效迁移。

## 2. 冻结机器人与策略接口

- 硬件范围：标准 PiPER 六轴机械臂与官方夹爪；不自动兼容其他型号或固件。
- observation/action：`joint_1.pos`…`joint_6.pos`（rad）与 `gripper.pos`（官方夹爪总开口宽度，m）。
- 首版策略状态不含 velocity、effort 或 TCP pose。唯一图像键为 `observation.images.d435`，单台 D435 的 RGB、H×W×3、uint8；action 与 observation.state 仍各为 7 维。深度用于相机验收和标定，不进入首版 ACT/SmolVLA/结果模型输入。
- 固定外置视角，在试采集前确认抓取点、夹爪与目标区域可见；准确安装位置、序列号、分辨率、帧率、曝光与标定在现场冻结。ACT 与 SmolVLA 使用相同原始 RGB/状态契约；各自官方预处理及统计随模型保存，不强行统一其不同归一化方式。
- 故障码、接收频率和时间戳单独记录；相机帧与机器人状态/动作的主机时间映射必须实测，不把单相机或 nominal fps 相同写成同步已经通过。
- Xbox processor 在数据写入前将 XYZ+yaw+夹爪增量转换为唯一 7 维 action schema；roll/pitch 每次会话开始时锁定。
- observation/action schema 若在 20 条试轨迹审计后改变，必须在正式任务数据开始前创建 amendment；正式数据开始后改变则新建 major 数据版本和预注册。

## 3. 任务与结果语义

### 任务 A：多位置抓取放置

成功：在冻结时限内抓取正确对象，将其完全释放并稳定留在目标区域；无策略导致的故障锁存、人工接管或越界动作。失败包括漏抓、掉落、错误区域、未完全释放、超时、策略故障或人工接管。

### 任务 B：语言条件分类放置

成功：执行对象与指令语义一致，并满足任务 A 的稳定放置与安全条件。失败包括错误对象/类别、未稳定放置、超时、策略故障或人工接管。

### 任务 C：接触敏感任务

任务 C 保留条件范围。2026-10-04 只有在 PiPER 安全采集链路通过、任务 A 有可重复 ACT 基线、且不会挤占 ACT、sim2real 核心对照、结果模型与冻结评测时才进入正式范围。通过后在查看任务 C 正式结果前创建 amendment，冻结堆叠、精确放置或宽松公差插入中的唯一任务及 100—150 条计划；未通过则只作探索或展示，不纳入最低成功标准。

## 4. 指标与统计

- 任务 A：逐回合二元成功率。
- 任务 B：指令一致完整成功率，以及相同预算下 SmolVLA−ACT 差值。
- 结果模型离线：AUPRC 为主，同时报告 AUROC、Brier score、校准误差、失败类型召回与提前量。
- 结果模型闭环：任务成功率和有害失败率共同为主，报告 rescue 与 harmful abort。
- 二元成功率报告 95% Wilson 置信区间；跨条件按冻结的任务×条件单元等权；模型差值使用分层 bootstrap。
- 次指标：ID/OOD 差、完成时间、接管率、failure taxonomy、感知/模型/通信/发送/总闭环 P50/P95/P99、控制抖动、推理显存、吞吐和训练资源。

结果模型工程门槛在 validation 前结合硬件基线冻结；当前不预填成功声明。

## 5. 划分与冻结 gate

| 划分 | 唯一用途 | 规则 |
|---|---|---|
| train | 模型拟合 | 使用冻结清洗规则 |
| development | 实现、超参数和失败诊断 | 可重复使用；正式调参有实验 ID |
| validation | checkpoint、阈值和 gate 决策 | 访问须记录；不回流同版本 train |
| final test | 冻结方案的一次性报告 | 阶段 5 封存，阶段 6 才运行 |

- 同一轨迹、裁剪片段、采集 session 和强相关 source trajectory 不跨划分。
- 20 条试轨迹只用于链路/质量审计，不默认进入正式数据。
- 正式采集前冻结 A/B 对象、位置、背景/光照、单台 D435 布局、语言模板、容差、时限、划分与最低预算。单视角抓取遮挡计入失败分析，短时遮挡评测继续保留，不靠添加视角或删除困难回合降低难度。
- 关键观测消融采用同一 RGB 下的状态或历史信息消融；不设置第二视角条件。任务 C gate 额外核对该任务在单视角下能否可靠观测与判定成功，其他 gate 不变。
- 正式数据、模型或配置若仍含其他相机键，不做补零、复制图像或别名转换；保留原始工件与登记，使用本预注册重新建立匹配的数据/模型版本。
- 阶段 5 结束前生成 final freeze，绑定具体 final test 清单和全部资产哈希。
- 策略造成的通信异常、故障锁存、超时或相机失败计为失败。只有预注册覆盖的外部故障可标无效，且保留日志/视频并按冻结顺序补跑。

## 6. 硬件与安全 gate

motion 前必须完成：

1. 标准 PiPER 铭牌、序列号、准确固件、官方夹爪、电源/线束和官方 USB-CAN 清点；
2. 物理急停存在且现场有效；底座固定、负载和工作区明确；
3. 五次只读连接可稳定读取六轴和夹爪反馈；单位、频率、时间戳新鲜度正确；
4. 通信中断、watchdog 和停止动作经现场验证，不导致机械臂坠落；
5. 冻结硬件身份、关节/夹爪限位、单步增量、反馈超时及工作空间；
6. motion 配置与验收记录完全匹配。

实机身份不符、急停缺失或停止策略不成立时 motion 保持关闭，不自动切换型号、固件或 SDK。

## 7. 停止规则

1. 未解释越界、零位漂移、急停失效、停止导致下落或通信异常时立即停止，修复并重新通过 gate。
2. A/B 各先采 20 条并审计，再扩到 50 条；每新增 100 条高质量示范后 development 主指标提升小于 5 个百分点时暂停扩采并诊断。
3. 每个训练 spec 预先冻结 `max_steps`、`max_wallclock_seconds`、`eval_interval_steps`、`patience_evaluations` 和 `stop_metric`；不得根据 final test 延长。
4. SmolVLA 保留必要公平对照；最多两轮预登记调参是上限而非必须完成的轮次。先做一轮，仅有明确 development 证据且不挤占 ACT/sim2real/结果模型时再安排第二轮；不扩大 VLA 型号搜索。
5. experiment identity 完全一致才可沿用 attempt；代码、配置、seed 或数据变化必须新建实验。旧 checkpoint 作为 `warm_start` 时显式登记。
6. 所有失败、拒绝和提前终止生成结果摘要与 `stop_reason`，不得从索引删除。

## 8. 最终冻结与禁调项

final freeze 至少绑定：本预注册、划分/评测协议、Git commit、环境、配置、seed、停止参数、数据版本/清单、checkpoint、归一化、标定、相机、硬件/安全配置、action chunk、阈值、回合预算、运行顺序、随机化、成功定义、指标、置信区间、failure taxonomy、排除规则和 final test 清单。

查看 final test 后禁止调整模型、checkpoint、seed、数据、阈值、action chunk、归一化、相机、标定、观测、动作、语言、任务容差、清洗、划分、指标、统计、failure taxonomy、预算、顺序、排除规则、条件权重或展示视频。实现缺陷使评测无效时保留原结果，建立新预注册/freeze 并使用未接触 holdout。

## 9. 待冻结字段

| 字段 | 最迟时间 |
|---|---|
| 实机身份、固件、夹爪范围、急停与停止策略 | motion 前 |
| 关节/夹爪限位、单步增量、反馈/watchdog 和工作空间 | motion 前 |
| Xbox GUID、轴、方向、deadzone、频率与步长 | 遥操作前 |
| A/B 对象、类别、位置、容器、背景、光照、相机和标定 | 正式采集前 |
| 语言模板、OOD、成功容差、时限和最低回合数 | 正式采集前 |
| 任务 C 形式与预算 | 10-04 gate 通过后的 amendment |
| 结果模型阈值与闭环门槛 | validation 结束前 |
| final test 清单及全部资产哈希 | 阶段 5 结束前 |

超过 gate 未冻结则不得开始相关运动、正式采集或评测。


## 10. sim2real 实验设计

### 10.1 最小里程碑

- S0 模型对齐：单 PiPER、官方夹爪、桌面及单虚拟 RGB 相机；核对关节顺序/方向/零位、法兰/TCP、夹爪总宽度、动作限速/响应和相机几何。提供可重复场景和误差报告，不把 URDF 导入当动力学已标定。
- S1 运动复现：仿真重现已记录的基础关节往返，与相同输入的实机反馈比较；纯动画和复播相同关节角不等于策略迁移。
- S2 到达策略：在仿真训练一个空载到达策略并进行实机验证，报告误差、成功率和失败原因。算法选择、观测、奖励或示范来源与预算在实现前明确；不默认扩成多套 RL 基准。
- S3 核心抓取迁移：任务 A 使用官方 ACT，完成下列三组对照。S2 不替代 S3 的视觉迁移验收。

### 10.2 三组核心对照

| 组别 | 学习数据 | 实机适配 | 对照目的 |
|---|---|---|---|
| R：真实数据 ACT | 固定真实 train 子集 | 按该组预注册训练 | 实机学习基线 |
| S：仿真训练 ACT | 仿真 train 示范 | 不使用真实示范梯度更新 | 测量直接迁移 |
| S+R：仿真预训练 ACT + 真实微调 | 与 S 同源仿真数据，再用与 R 完全相同的真实子集 | 明确微调预算 | 比较相同真实数据量下的收益 |

R 与 S+R 使用相同真实轨迹 ID 和嵌套数据规模、相同公共 RGB/七维 schema、架构及可比的候选选择规则。分别报告仿真预训练算力、真实微调算力、总算力、真实示范数量/时长、调参与标定成本；不能把相同真实数据量称为总训练成本相同。S 可使用冻结的 train/development 硬件标定信息，但不得使用实机任务示范训练或 final test 反馈调参；报告中称“无真实示范微调”，不用含糊的“从未接触真实世界”。

所有组使用同一真实任务 A 成功定义、冻结条件单元、回合预算及统计方法；实机顺序配平/随机化。仿真成功率和实机成功率分列；失败不得排除。至少报告 S−R、(S+R)−R 的实机成功率差值及置信区间。仿真/实机任务定义一致且条件配对时才计算域间差值，并注明比较口径。

### 10.3 来源、划分与参数冻结

记录 domain=sim/real、仿真器精确版本、场景/资产来源版本、控制周期/子步、执行器与接触参数、虚拟相机、随机化范围与 seed、生成策略/示范来源、仿真/真实父数据版本、预训练与微调阶段。上述字段位于 Manifest/spec/旁路遥测，不加入七维策略向量。

同一场景族、源轨迹、仿真随机化派生样本与对应真实采集 session 按 lineage 分组；不因 domain 不同而绕开泄漏限制。用来系统辨识/相机标定/随机化调参的真实记录属于 train/development，并披露成本；不得用 validation/final test 优化仿真器后重新当独立测试。新增图像/状态输入需单独 amendment。

真实动作契约仍为六轴 rad 与夹爪总宽度 m。S2 可在单独学习环境中使用目标位置等额外观测，但需明确标记 reach 专项配置和数据，不能混入任务 A/B 的七维公共策略输入；任何实机输出仍转换为完整合法七维目标并经过现有控制边界。

仿真时间、墙钟执行时间和设备接收时间分域保存；仿真数据不伪造 SDK 调用成功、D435 序列号或实机 acceptance。未来采集适配实现需单独批准，不修改现有审计工具去接受伪硬件遥测。

## 11. sim2real 验收与停止

- 仿真器版本、资产转换、关节/相机对齐容差、控制响应/接触参数、随机化范围、训练算法、seed 数、预算与实机回合数均待冻结，不填猜测阈值。
- S0/S1 软件验证不授予 CAN/实机权限；S2/S3 实机执行仍需原硬件、停止和运动门禁，仿真通过不能替代防碰撞或停止保护。
- 数值失稳、方向/单位不符、控制延迟/限位不匹配时停止迁移，先修复对应映射；不通过裁剪或自动恢复掩盖问题。
- 仿真线最低交付包含可复现场景、对齐报告、至少一个已进行实机验证的仿真训练策略，以及 S3 三组对照的完整结果。迁移无收益时保留负结论；只完成 S2 或展示视频不算整条线闭合。
- 结果模型可以用仿真扰动补充样本，但模拟失败标签不是实际失败真值；真实校准和闭环评测仍独立完成。

## 12. 新增待冻结字段

| 字段 | 最迟时间 |
|---|---|
| 仿真器/资产精确版本、许可证、控制与传感器模型、S0 误差容差 | 仿真模型对齐验收前 |
| S2 算法、观测/动作变换、奖励或示范来源、sim 与 real 验证预算 | 到达策略训练前 |
| S3 R/S/S+R 数据 lineage、真实子集、随机化、统计、seed/算力与回合预算 | 第一组核心迁移训练前 |
| 结果模型使用仿真样本的标签、域划分和真实校准方案 | 对应模型训练前 |
| 仿真资产/随机化/校准来源、全部阶段 checkpoint 与真实测试清单 | final freeze 前 |

## 13. 批准与运行绑定边界

用户于 2026-09-08 批准本轮文档规划，明确代码实现另行批准。当前未安装 MuJoCo、未创建仿真代码/运行配置、未训练或运动。实施排期均以代码批准和前置 gate 为条件，批准等待不计技术完成，也不通过增加每周工作量追赶。

本文件是规划侧唯一活动研究协议。实现仓库原 `configs/project.json`、doctor 预注册绑定与既有 release 仍指向 PR-20260906-01；本轮不改这些运行配置或旧快照。新增协议可作为文档镜像保存，正式新实验前须在获批代码/配置变更中切换绑定并验证，再形成相应 release/run 来源。旧运行按旧协议审计，不追溯套用本修订。
